pipeline {
    agent any

    stages {
        stage('Hola Dios') {
            steps {
                echo "Hola Dios, llamen a Dios, desde la rama ${env.BRANCH_NAME}  y el commit ${env.GIT_COMMIT}"
            }
        }
    }
}
